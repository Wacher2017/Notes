1、函数返回值
   ·什么是函数的返回值
      ·函数的执行结果
      ·可以没有return
    ·一个函数对应只返回一种类型的值

2、函数传参
  ·可变参（不定参）：arguments
  ·示例1：求和（求所有参数的和）
  ·示例2：CSS函数
    ·判断arguments.length
    ·给参数取名，增强可读性
  ·取非行间样式（不能来设置）：
    ·obj.currentSytle[attr]
    ·getComputedStyle(obj, false)[attr]

3、数组基础
  ·数组使用
  ·数组的属性
    ·length （即可用获取，又可以设置，还可以快速清空数据）
  ·数组的使用原则：数组中应该只存一种类型的变量
  ·数组的方法
    ·添加：push(元素)【从尾部添加】、unshift(元素)【从头部添加】
    ·删除：pop()【从尾部弹出】、shift()【从头部弹出】
    ·排序：sort()
    ·转换类：concat(数组2)【连接两个数组】、join(分隔符)【用分隔符组合数组元素生成字符串】
    ·插入、删除：splice(开始, 长度, 元素...)【先删除后插入】、splice(开始,长度)【删除】、splice(开始,0,元素...)【插入】